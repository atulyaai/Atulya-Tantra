# NP-DNA Nano Sample

This is a compact sample checkpoint for loading and dashboard evaluation. It
contains the model weights, tokenizer, metadata, and cortex projections needed
by `NpDnaCore.load("tantra/outputs/npdna_nano")`.

Local training logs, run state, backups, and version snapshots are intentionally
not included.
